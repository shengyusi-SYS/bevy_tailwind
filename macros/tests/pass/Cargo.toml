[package]
name = "test_pass"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
bevy = { version = "0.18.0", default-features = false, features = [
    "bevy_ui",
    "bevy_text",
] }
bevy_tailwind_macro = { path = "../../" }
bevy_tailwind = { path = "../../../" }
