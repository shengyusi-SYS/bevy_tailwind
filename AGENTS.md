AI 工作流程

1. **理解任务**：明确任务范围和目标
2. **制定计划**：在 todo list 中列出具体步骤
3. **逐步执行**：按 todo list 逐项完成，每完成一项立即标记
4. 在完成较大量任务后，重新阅读本文档，明确工作规范
5. 记住：**现有任务结束前向终端发送内容为空的 `echo` 命令确认下一步！！！!!!**

其它要求：

- 如果命令中提出新方案，先对方案做分析，不立即修改，分析完成后向终端发送内容为空的 `echo` 命令确认是否执行
- 如果在尝试运行编译测试命令时，得到的是新指令，那么默认视为编译运行通过
